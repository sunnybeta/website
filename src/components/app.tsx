import About from './about';
import Header from './header';

export default function App() {
	return (
		<main>
			<Header />
			Website is still under construction :(.
			Don't bother with the links in the navbar.
			<About />
		</main>
	);
}
